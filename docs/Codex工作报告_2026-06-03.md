# Codex 工作报告

> 记录日期：2026-06-03  
> 工作范围：第十四周项目收口、课程补件、HTML 展示页增强、README 回调、分支合并与 GitHub 同步

## 1. 工作目标

本轮工作的目标主要有四类：

1. 修复项目中会影响复现和口径一致性的关键问题。
2. 按课程进度表补齐第 21-32 次课对应的过程产物与归档材料。
3. 将 `项目展示.html` 提升为可用于答辩的主展示页。
4. 在尽量不破坏原有成果的前提下，整理分支、回滚不满意改动，并最终合并到 `main` 推送 GitHub。

## 2. 代码与复现链修复

### 2.1 数据口径修复

修复了以下脚本中“误混入非主批次数据文件”的问题：

- `src/create_screening.py`
- `src/burst_detection.py`

核心处理方式：

- 不再简单读取 `Data/` 下所有 `*.txt`
- 改为只读取主数据批次：
  - `download_1-500.txt`
  - `download_501-643.txt`
- 增加有效 WoS 记录过滤逻辑，避免异常文本混入

这一步解决了此前“脚本复跑后统计从 643 偏移到 792”的风险。

### 2.2 运行入口与依赖说明

调整了以下文件：

- `run_pipeline.py`
- `run_pipeline.js`
- `requirements.txt`

主要内容：

- 补充复现链说明
- 统一 `643` 全量集与 `147` DL 纯净集的角色分工
- 补充缺失依赖说明

## 3. 课程补件与归档文档

按课堂进度表补充或整理了以下文档：

- `docs/课堂进度对照矩阵.md`
- `docs/引用库与AI伦理说明.md`
- `docs/智能体工作流总览.md`
- `docs/写作修订日志.md`
- `docs/预答辩与PR-Review准备包.md`
- `docs/终版归档说明.md`
- `docs/release_v1.1_draft.md`

这些文档的作用分别包括：

- 将第 21-32 次课要求与现有成果逐项对照
- 说明引用来源、AI 使用边界与人工复核方式
- 解释人机协作流程和提示词/底稿的组织方式
- 为预答辩、PR Review、课程归档提供可直接展示的材料

## 4. HTML 展示页工作

### 4.1 结构增强

在 `项目展示.html` 中完成了较大幅度的答辩化增强，重点包括：

- 新增“本研究的中心判断”页
- 新增“证据链与方法可信度”页
- 新增“里程碑文献”页
- 新增“升级版技术路线图”页
- 强化结论页中的贡献表达
- 补充课程成果与展示完整度相关表述

这部分的目标是让 HTML 不再只是成果陈列，而能承担“现场答辩主讲材料”的功能。

### 4.2 口径统一

同步修正了突现词相关叙述：

- `src/burst_visualize.py`
- `outputs/keyword_burst_report.md`
- `README.md`
- `项目展示.html`

统一为：

- 全量主数据集检出 `97` 个原始突现词
- 页面展示与课程讲述聚焦 `Top 25` 核心突现词

### 4.3 文案微调与回滚

后续对 `项目展示.html` 做过两轮语言层面的收口：

1. 一轮是“降低过深、过满、过论文式表达”
2. 一轮是基于 `humanizer-zh` 思路进一步去除 AI 痕迹

其中第二轮用户不满意，因此已执行回滚：

- 回滚提交：`adea2c1`
- 回滚内容：撤销 `Humanize HTML defense narration`

最终保留的是“较自然但不过度人造”的上一版 HTML。

## 5. README 调整

对 `README.md` 做过两类处理：

### 5.1 必要的事实修正

- 保留了 `97 / Top 25` 的突现词口径说明
- 保留了对当前展示入口的适度补充

### 5.2 回调主页风格

因为后续版本的 README 变得过于“总入口化”，与原本首页气质不一致，所以又做了回调：

- 删除大块“快速入口”
- 删除“当前交付策略”整段
- 将目录说明收回到更简洁的主页风格

对应提交：

- `decc72d` `Restore README homepage tone`

## 6. 分支、提交与同步操作

### 6.1 工作分支

本轮主要工作分支：

- `codex/week14-project-audit-fixes`

### 6.2 关键提交

本轮涉及的关键提交包括：

- `5c3f145` 修复复现链与口径问题
- `f516bd4` 完成 HTML 答辩包与课程补件
- `4b5582f` 收软 HTML 讲述语气并补充 Codex 致谢
- `b8863e2` 一次后续 humanizer 式文案收口
- `adea2c1` 回滚上一条 humanizer 式收口
- `decc72d` 调回 README 首页风格

### 6.3 推送与合并

执行了以下同步动作：

1. 将 `codex/week14-project-audit-fixes` 推送到 GitHub
2. 检查 `main` 与分支差异，确认是可安全快进合并
3. 切换到 `main`
4. 使用 `git merge --ff-only codex/week14-project-audit-fixes`
5. 将合并后的 `main` 推送到 GitHub

因此目前 GitHub 项目主页默认看到的已经是本轮整理后的版本。

## 7. 备份与清理

为了保证 `项目展示.html` 可回退，创建了本地备份：

- `工作底稿/1_意见反馈/项目展示备份/项目展示_2026-06-03_takeaway收口前备份.html`

此外还处理过一个临时安装的 skill：

- 安装：`D:\codex\skills\humanizer-zh`
- 后续按用户要求删除：`D:\codex\skills\humanizer-zh`

## 8. 验证情况

实际做过的验证包括：

- `python src/create_screening.py`
  - 验证解析口径稳定回到 `643`
- `python src/burst_detection.py`
  - 验证突现检测基于 `643` 主数据集
- `python run_pipeline.py`
- `node run_pipeline.js`

未完全完成的验证：

- 试图做一次本地 HTML 自动化可视化检查
- 由于本地缺少 `playwright` 运行时，没有伪造“已视觉验收”的结论

## 9. 当前结果概览

到本报告生成时，项目已经完成了以下状态转变：

- 从“成果很多但口径与说明略散”收口为“主线较清晰、课程材料较完整”的状态
- 从“HTML 更像展示页”提升为“HTML 可以承担答辩主讲”的状态
- 从“分支内完成”推进到“已合并 main 并同步 GitHub”的状态

同时也保留了用户明确偏好的内容：

- README 不做大改，保留原有主页感
- `项目展示.html` 不保留过重的 humanizer 风格收口
- Codex 致谢保留在展示页中

## 10. 仍可继续做的事

如果后续还要继续推进，可以优先做这几件事：

1. 继续人工精修 `项目展示.html` 的个别讲解句
2. 检查 GitHub Pages 展示是否完全符合当前答辩版内容
3. 补一份“教师视角严格审查”结果，作为答辩前自查材料
4. 如有需要，再单独准备轻量级 PPT 备份版

